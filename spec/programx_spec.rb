# frozen_string_literal: true

RSpec.describe Programx do
  it 'has a version number' do
    expect(Programx::VERSION).not_to be_nil
  end

  it 'does something useful' do
    ret = false
    expect(ret).to be(true)
  end
end
